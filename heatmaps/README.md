# Heatmaps

This directory is populated by `notebooks/create_heatmaps.ipynb`.

It contains spatial heatmap figures showing bubble density and size distributions across the bioreactor cross-section for each operating condition and camera placement.
